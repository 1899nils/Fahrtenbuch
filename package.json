{
  "name": "fahrtenbuch",
  "private": true,
  "version": "1.0.0",
  "description": "Fahrtenbuch Monorepo",
  "scripts": {
    "start": "node backend/server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
